import Page from "@/components/pageWrapper";
import { AuthPage } from "@/components/ui/auth-page";

const page = () => {
  return (
    <main>
      <AuthPage />
    </main>
  );
};

export default page;
